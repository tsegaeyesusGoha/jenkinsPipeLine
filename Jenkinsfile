/* Requires the Docker Pipeline plugin */
pipeline {
    stages {
        stage('build') {
            steps {
                sh 'mvn --version'
                sh 'echo "Hello World"'
            }
        }
    }
}
